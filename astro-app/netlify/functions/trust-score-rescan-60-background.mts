/** Explicit day-60 URL. The old bridge URL remains an alias for compatibility. */
import { createTrustScoreRescanHandler } from "./trust-score-rescan-core.mts";

export default createTrustScoreRescanHandler("60");
